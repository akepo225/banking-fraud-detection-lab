# v0.1 Publication Gate Audit

Status: ready for human review.

<!-- HITL-REVIEW-REQUIRED: Repo owner should review this audit and make the final public-release decision. This report does not publish the repository. -->

## Decision

**Hold pending final human publication action.**

The v0.1 implementation evidence is strong enough for final publication review:
the clean-checkout setup, lint, full test suite, featured notebook smoke tests,
case library validators, regulatory source validators, README/license checks,
and GitHub CI are passing. The repository should remain private until the repo
owner accepts this HITL audit and explicitly decides to publish.

No automatic publishing action was taken.

## Verification Commands

Commands were run from a fresh `feat/issue-14-implementation` worktree created
from `origin/main` after PR #43 merged.

| Command | Result | Evidence |
| --- | --- | --- |
| `uv sync --extra dev` | Pass | Installed the project and dev dependencies in a fresh worktree. |
| `uv run ruff check .` | Pass | `All checks passed!` |
| `uv run pytest tests/test_foundations_notebook.py tests/test_private_banking_notebook.py tests/test_digital_scam_to_mule_notebook.py tests/test_alert_governance_notebook.py` | Pass | `4 passed in 74.03s`; each featured v0.1 notebook executed through its smoke test. |
| `uv run pytest` | Pass | `111 passed in 53.55s`; includes generator, schema, SQLite, case-library, regulatory-source, publication-doc, CI-gate, and notebook smoke tests. |
| `gh run list --branch main --workflow CI --limit 5` | Pass | Latest `main` CI run for PR #43 merge completed with `success` on 2026-05-24. |
| `rg -n -i "Julius Baer|job-preparation|job preparation|portfolio project|portfolio presentation|recruiters|official training|affiliation|affiliated|endorsement|real client data|confidential|proprietary|reconstruct|reconstruction|legal advice|compliance advice|must comply|must report|legally required|The public curriculum repository|warmups/|create_sqlite data/sample banking_fraud_lab.sqlite" --glob '!.venv/**' .` | Reviewed | Matches are glossary guardrails, disclaimers, contribution rules, validator patterns, explicit case/regulatory limitations, one fraud-context `recruiters` mention in the money-mule source pack, tests, and this audit log. No unexpected personal framing, stale missing-directory reference, broken README command, real-client-data claim, real-bank affiliation claim, reconstruction claim, or legal-advice instruction was found. |

## Publication Checklist Review

| Gate | Status | Evidence |
| --- | --- | --- |
| Private pre-publication posture | Pass | `README.md` states the project is a private pre-publication curriculum and says the repository remains private until the v0.1 publication gate is reviewed. `docs/release/v0.1-publication-checklist.md` repeats this boundary. |
| No personal job-preparation material or real client data | Pass | Search results show personal-framing terms only in avoidance guidance, guardrails, roadmap/checklist gates, publication tests, and this audit's resolved issue log. Sample data is under `data/sample/` and is generated synthetic CSV data. |
| README, disclaimer, split licensing, ADR, and contribution guidance | Pass | `README.md`, `LICENSE.md`, `LICENSES/CODE-MIT.md`, `LICENSES/CONTENT-CC-BY-NC-4.0.md`, `CONTRIBUTING.md`, and ADR-0001 are present and linked. `tests/test_publication_docs.py` checks README, license, checklist, and disclaimer coverage. |
| Realistic synthetic data model and data dictionary | Pass | `docs/schema/README.md` and `docs/schema/data_dictionary.md` document the generated tables. `tests/test_schema_contract.py` verifies generated table columns/types and data-dictionary alignment. |
| Deterministic generator and sample data | Pass | `data/sample/` contains committed tiny sample data. `tests/test_generator_determinism.py` verifies fixed-seed determinism and seed variation. `tests/test_generator_entities.py` verifies required v0.1 entities and join integrity. |
| SQLite loader supports first SQL exercises | Pass | `src/banking_fraud_lab/sqlite_loader.py`, `src/banking_fraud_lab/create_sqlite.py`, `sql/README.md`, and `sql/examples/` are present. `tests/test_sqlite_loader.py` verifies SQLite loading, foreign keys, and representative SQL execution. |
| Featured notebooks run end-to-end on tiny data | Pass | Focused notebook smoke run passed with `4 passed in 74.03s`. The notebooks are `00_foundations`, `01_private_banking_transaction_fraud`, `02_digital_scam_to_mule`, and `03_alert_governance`. |
| Seeded case library and regulatory source index | Pass | `docs/cases/index.md`, `docs/cases/source_packs/`, `docs/regulation/index.md`, and `docs/regulation/source_notes/` are present. `tests/test_case_library_metadata.py` and `tests/test_regulatory_source_index.py` validate metadata, official links, learning implications, linked modules, no direct quote blocks, and no imperative compliance wording. |
| Tests cover v0.1 quality gates | Pass | `docs/quality_gates/v0.1-ci.md` maps required v0.1 quality gates to concrete tests. `tests/test_ci_quality_gates.py` verifies the manifest cites existing tests and that featured notebook smoke tests use `nbconvert --execute`. |
| Lightweight CI runs linting and tests | Pass | `.github/workflows/ci.yml` runs `uv sync --extra dev`, `uv run ruff check .`, and unfiltered `uv run pytest`. `tests/test_ci_quality_gates.py` enforces this command sequence. Latest `main` CI after PR #43 merge completed successfully. |

## Issue #14 Acceptance Review

| Acceptance criterion | Status | Evidence |
| --- | --- | --- |
| The v0.1 publication checklist is reviewed item by item. | Pass | See the checklist review table above. |
| CI passes on a clean checkout. | Pass | Latest `main` CI run after PR #43 merge succeeded; local fresh worktree ran the same setup/lint/full-test commands successfully. |
| Featured notebooks run from a clean setup on tiny sample data. | Pass | Focused notebook smoke test run passed with `4 passed in 74.03s`; full suite also passed. |
| The case library and regulatory source index are present and source-disciplined. | Pass | Case and regulatory docs are present and covered by validation tests in the full suite. Search results show source-discipline language and no unexpected legal-advice/reconstruction claims. |
| The repo is checked for personal job-preparation material, real client data, real-bank affiliation claims, and legal-advice language. | Pass | The focused `rg` search found only glossary guardrails, disclaimers, contribution rules, validator patterns, explicit limitations, tests, and this audit's resolved issue log. |
| A publish-or-hold decision is recorded without publishing automatically. | Pass | Decision is **Hold pending final human publication action**. No public-release or repository-publication action was taken. |

## Supplemental End-User QA Findings

This follow-up QA pass tested documented commands and reviewed docs as an end
user would before accepting the final handoff.

| Finding | Severity | Resolution |
| --- | --- | --- |
| README SQLite command used two positional arguments: `data/sample banking_fraud_lab.sqlite`. The CLI accepts one optional database path, so the command failed with `unrecognized arguments: banking_fraud_lab.sqlite`. | Warning | Fixed README to use `uv run python -m banking_fraud_lab.create_sqlite data/sample/minimal_world.sqlite`, matching the SQL guide and verified CLI behavior. |
| Future roadmap wording used personal/job-market framing: `portfolio project`, `Portfolio presentation template`, `recruiters`, and `end-to-end portfolio project`. | Warning | Reworded those future-roadmap items to capstone/public-reader language and expanded the publication-risk search terms to cover the phrases. |
| `CONTEXT.md` described the project as a public curriculum repository in present tense while the v0.1 gate is still under review. | Info | Reworded the glossary definition to describe the repository as pre-publication until the v0.1 publication gate is accepted. |
| `notebooks/README.md` listed notebook order but did not tell a new user how to launch or batch-execute the notebooks. | Warning | Added `uv run jupyter lab notebooks` and the focused notebook smoke-test command. |
| `notebooks/README.md` listed optional `warmups/` material, but no `notebooks/warmups/` directory exists. | Minor | Removed the stale optional directory reference. |
| `sql/README.md` listed representative SQL files but did not show how to run them against the generated database. | Warning | Added a SQLite `.read` command for a single example and the focused test command that verifies all representative examples. |
| Data-model consistency review found no schema/sample/foreign-key/protected-label mismatches. | Clean | Independent review verified sample CSV headers, foreign keys, learner-facing SQLite table exclusion, Client/User/Partner separation, and Banking relationship terminology. |

## Residual Human Review Items

- Confirm whether to accept the conservative hold decision or publish publicly.
- Confirm README positioning for the intended **Target learner**.
- Confirm disclaimer wording, split license posture, and source framing.
- Confirm case-library and regulatory-note framing before any public release.
