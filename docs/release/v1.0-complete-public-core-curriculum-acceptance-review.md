# v1.0 Complete Public Core Curriculum Acceptance Review

Status: draft — v1.0 acceptance review pending HITL sign-off.

<!-- HITL-REVIEW-REQUIRED: Repo owner must sign off this v1.0 acceptance review, the frozen 10-module core list, the v1.1–v1.4 extension boundary, and the hardening-only publication posture before v1.0 is accepted. Accepting v1.0 does not publish the repository; the go-public switch remains the separate v0.1 publication gate (docs/release/v0.1-publication-checklist.md). -->

## Decision

**v1.0 hardens the v0.9 beta path rather than expanding feature scope.** This is a
stabilization / publication-readiness release, **not** a feature release: per the v0.9
acceptance review (`docs/release/v0.9-capstone-beta-acceptance-review.md` § Hand-Off To
v1.0) and the v1.0 scope boundary (`docs/release/v1.0-scope.md`, issue #249), v1.0 accepts
**no** new features, **Detection patterns**, scenario families, schema entities, or advanced
infrastructure. The work is **hardening only**: every frozen core module runs end-to-end,
the synthetic-data / SQLite / schema invariants hold, the public-facing language and the
v1.1–v1.4 extension boundary are mechanically guarded, and the case library + regulatory
source index cover the frozen core.

The repository remains private until the separate v0.1 publication gate is accepted. v1.0
adds no public-release / "we are now public" language and makes no production-readiness or
certification claim. Both **Fraud detection tracks** — Alpine Crest Private Bank
(private-banking) and NovaBank Digital (digital-banking) — remain first-class throughout the
frozen core, supported by the single **Realistic synthetic data model** and its
**Progressive data views**.

This review is the v1.0 acceptance artifact. It is currently a draft pending human sign-off
(`HITL-REVIEW-REQUIRED`); the status flips to `HITL-REVIEW-COMPLETE` once the repo owner
approves the frozen core list, the extension boundary, and the hardening-only publication
posture.

## Scope And Parent Issue

This release closes the v1.0 workstream under parent issue **#53** (PRD: Banking Fraud
Detection Lab v1.0, `docs/prds/v1.0-complete-public-core-curriculum.md`) and the scope +
triage gate **#249** (`docs/release/v1.0-scope.md`). The v1.0 structural slices, their PRs,
and what each delivers:

| Slice | Title | Delivered | HITL |
| --- | --- | --- | --- |
| #250 | Core-module runnability acceptance gate | `tests/test_v10_acceptance_review.py` (frozen-core presence + per-module runnability + cross-version regression loop) + this review draft | |
| #252 | Synthetic-data, SQLite, and schema validation gate | `tests/test_v10_data_schema_gate.py` (reuses `test_schema_contract`, `test_progressive_views`, `test_sqlite_loader` without weakening them) | |
| #253 | Publication-language and extension-boundary guardrails | `tests/test_publication_docs.py` v1.0 terminology block + structural v1.1–v1.4 boundary guard | |
| #254 | Case library and regulatory source index coverage gate | `tests/test_case_library_metadata.py` frozen-core inbound-link coverage | |
| #251 | Framing-document consistency audit | `docs/ROADMAP.md` module-06 naming reconciliation + v1.0 doc-link coverage tests | |
| #255 | v1.0 acceptance review, release checklist, and publication decision | this review (expanded) + `docs/release/v1.0-release-checklist.md` + comprehensive review-content tests | **HITL (pending sign-off)** |

## PRD Deliverable Evidence

v1.0 adds no new deliverables; it hardens and mechanically gates the v0.1–v0.9 deliverables
that already satisfy the PRD (#53).

| PRD deliverable (issue #53) | Evidence |
| --- | --- |
| Complete frozen core curriculum 00–09 | `notebooks/00_foundations` … `notebooks/09_capstone`; `tests/test_v10_acceptance_review.py` frozen-core + per-module runnability |
| Realistic synthetic data model reused throughout | `src/banking_fraud_lab/` schema; `tests/test_schema_contract.py`; `tests/test_v10_data_schema_gate.py` |
| Progressive data views per module | `src/banking_fraud_lab/progressive_views.py`; `docs/schema/progressive_views.md`; `tests/test_progressive_views.py`; `tests/test_v10_data_schema_gate.py` |
| Both fraud tracks first-class | Alpine Crest Private Bank (`notebooks/01,04,06,07,08,09`) + NovaBank Digital (`notebooks/02,05,06,07,08,09`) |
| SQLite-first exercises | `sql/examples/*.sql`; `tests/test_sqlite_loader.py`; `tests/test_v10_data_schema_gate.py` |
| Cases, regulatory notes, graph, governance, monitoring, capstone linked | `docs/cases/`, `docs/regulation/`, `notebooks/06,03,08,09`; `tests/test_case_library_metadata.py`; `tests/test_v10_acceptance_review.py` (#254 frozen-core coverage) |
| Consistent framing docs / licenses / disclaimers / ADRs | `README.md`, `CONTEXT.md`, `LICENSES/`, `docs/adr/`; `tests/test_publication_docs.py`; `docs/ROADMAP.md` module-06 reconciliation (#251) |
| Clear extension boundary (v1.1–v1.4 optional) | `docs/adr/0004-*.md`; `docs/ROADMAP.md` § Post-1.0; `tests/test_publication_docs.py` structural boundary guard (#253) |
| v1.0 release checklist | `docs/release/v1.0-release-checklist.md` (#255) |

## PRD User Story Acceptance Matrix

| PRD user story (#53) | Child issue / PR | Evidence files | Automated tests | Manual / HITL evidence |
| --- | --- | --- | --- | --- |
| User story 1: clear start path | #251 | `README.md`, `notebooks/README.md`, `sql/README.md` | `tests/test_publication_docs.py` (link coverage, README contract) | Reviewed: README → notebook guide → SQL guide reachability |
| User story 2: modules 00–09 run in order | #250 | `notebooks/00_foundations` … `notebooks/09_capstone` | `tests/test_v10_acceptance_review.py` (frozen-core presence + per-module runnability + cross-version regression) | Reviewed: every frozen module has a README, a featured notebook, and a covering smoke test |
| User story 3: Realistic synthetic data model reused | #252 | `src/banking_fraud_lab/` schema, `docs/schema/data_dictionary.md` | `tests/test_schema_contract.py`, `tests/test_v10_data_schema_gate.py` | Reviewed: additive-only schema (ADR-0002), no drift |
| User story 4: Progressive data views | #252 | `src/banking_fraud_lab/progressive_views.py`, `docs/schema/progressive_views.md` | `tests/test_progressive_views.py`, `tests/test_v10_data_schema_gate.py` | Reviewed: views conform and are documented |
| User story 5: both tracks complete | #250 | Alpine Crest + NovaBank notebooks across 01/02/04/05/06/07/08/09 | `tests/test_v10_acceptance_review.py` per-module runnability, `tests/test_publication_docs.py` glossary institutions | Reviewed: both **Fraud detection tracks** first-class |
| User story 6: cases / regulatory / graph / governance / monitoring / capstone linked | #254, #251 | `docs/cases/`, `docs/regulation/`, `notebooks/06,03,08,09` | `tests/test_case_library_metadata.py` (frozen-core inbound coverage), `tests/test_publication_docs.py` (link coverage) | Reviewed: every frozen core module has ≥1 inbound case/regulatory link |
| User story 7: clean-setup runnability | #250 | `.github/workflows/ci.yml`, `docs/quality_gates/v0.1-ci.md` | `tests/test_v10_acceptance_review.py` (cross-version regression re-runs v0.3–v0.9 notebooks) | Reviewed: full `uv run pytest` green on the worktree |
| User story 8: tiny committed, larger generated locally | #252 | `data/sample/` (seed-42 tiny), `src/banking_fraud_lab/` generator | `tests/test_schema_contract.py` (deterministic generation), `tests/test_v10_data_schema_gate.py` | Reviewed: only `data/sample/` committed; medium/large generated locally |
| User story 9: docs / licenses / disclaimers / roadmap / ADRs consistent | #251 | `README.md`, `docs/ROADMAP.md`, `CONTRIBUTING.md`, `LICENSES/`, `docs/adr/` | `tests/test_publication_docs.py` (link coverage + terminology), `tests/test_v10_acceptance_review.py` | Reviewed: ROADMAP module-06 drift reconciled; framing-doc links resolve |
| User story 10: clear extension boundaries | #253 | `docs/adr/0004-*.md`, `docs/ROADMAP.md` § Post-1.0, `docs/release/v1.0-scope.md` | `tests/test_publication_docs.py` (structural v1.1–v1.4 boundary guard) | Reviewed: v1.1–v1.4 confined to ROADMAP Post-1.0 + ADR-0004; no core module beyond 09 |
| User story 11: v1.0 checklist | #255 | `docs/release/v1.0-release-checklist.md` | `tests/test_v10_acceptance_review.py` (checklist presence + content) | HITL pending: maintainer signs the checklist gates |

## End-To-End Learner Path Evidence

A learner can walk the frozen core in order; each step has a featured notebook and a smoke
test, and the cross-version regression loop in `tests/test_v10_acceptance_review.py`
re-executes the v0.3–v0.9 notebook smoke tests end-to-end to prove the path still runs.

| Step | Evidence | Validation |
| --- | --- | --- |
| Foundations | `notebooks/00_foundations/` | `tests/test_foundations_notebook.py` |
| Private-banking transaction baseline | `notebooks/01_private_banking_transaction_fraud/` | `tests/test_private_banking_notebook.py` |
| Digital scam-to-mule baseline | `notebooks/02_digital_scam_to_mule/` | `tests/test_digital_scam_to_mule_notebook.py` |
| Alert governance | `notebooks/03_alert_governance/` | `tests/test_alert_governance_notebook.py` |
| Private-banking feature engineering | `notebooks/04_private_banking_feature_engineering/` | `tests/test_private_banking_feature_engineering_notebook.py`, `tests/test_private_banking_supervised_baseline_notebook.py` |
| Digital session and payment fraud | `notebooks/05_digital_session_and_payment_fraud/` | `tests/test_digital_feature_engineering_notebook.py`, `tests/test_digital_supervised_baseline_notebook.py` |
| Graph and network fraud | `notebooks/06_graph_network_fraud/` | `tests/test_alpine_crest_graph_notebook.py`, `tests/test_novabank_graph_notebook.py` |
| Interpretability and model risk | `notebooks/07_interpretability_model_risk/` | `tests/test_alpine_crest_interpretability_notebook.py`, `tests/test_novabank_interpretability_notebook.py`, `tests/test_governance_memo_notebook.py` |
| Production monitoring patterns | `notebooks/08_production_monitoring_patterns/` | `tests/test_alpine_crest_monitoring_notebook.py`, `tests/test_novabank_monitoring_notebook.py`, `tests/test_alert_review_governance_notebook.py` |
| Capstone | `notebooks/09_capstone/` | `tests/test_capstone_scoring_notebook.py`, `tests/test_capstone_synthesis_notebook.py` |

## Exit Criteria (PRD #53 / docs/ROADMAP.md)

- ✅ **All required modules have runnable featured notebooks** — every frozen 00–09 module
  has ≥1 featured notebook and a covering smoke test; the cross-version regression loop
  re-executes them. Enforced by `tests/test_v10_acceptance_review.py`.
- ✅ **Realistic synthetic data model supports all modules via Progressive data views** —
  enforced by `tests/test_schema_contract.py` and `tests/test_progressive_views.py`, gated
  by `tests/test_v10_data_schema_gate.py` (#252).
- ✅ **Tiny sample data committed; medium/large generated locally** — `data/sample/` is the
  seed-42 tiny profile; larger profiles stay out of git. Enforced by schema-contract +
  gitignore.
- ✅ **SQLite exercises for core feature extraction and investigation** — `sql/examples/*.sql`
  run against the generated database. Enforced by `tests/test_sqlite_loader.py`, gated by
  `tests/test_v10_data_schema_gate.py`.
- ✅ **Case library covers private banking, digital banking, regulatory/governance, and
  graph/network patterns** — enforced by `tests/test_case_library_metadata.py`; frozen-core
  inbound coverage by #254.
- ✅ **Regulatory source index covers Swiss anchors and selected EU/UK/global references** —
  enforced by `tests/test_case_library_metadata.py` regulatory-note structure checks.
- ✅ **Tests cover generator, schema contracts, SQL loading, evaluation, case metadata,
  regulatory structure, and notebook smoke** — the unfiltered `uv run pytest` runs all of
  them; the v1.0 gates (#250–#254) assert the invariant holders are wired in.
- ✅ **CI passes on a clean checkout** — `.github/workflows/ci.yml` runs `uv sync --extra dev`,
  `uv run ruff check .`, `uv run pytest`; enforced by `tests/test_ci_quality_gates.py`.
- ✅ **README, roadmap, contribution guide, disclaimers, licenses, and ADRs are consistent** —
  enforced by `tests/test_publication_docs.py`; ROADMAP module-06 drift reconciled by #251.
- ✅ **No real client data, no job-market framing, no real-bank affiliation claims, no
  legal advice** — enforced by the terminology guardrails in `tests/test_publication_docs.py`
  and the case-library/regulatory validators.

## Testing Decisions (PRD #53)

- ✅ Run full tests, linting, and notebook smoke checks on a clean checkout — the unfiltered
  `uv run pytest` plus `uv run ruff check .`.
- ✅ Validate deterministic sample regeneration and generated-data scale profiles —
  `tests/test_schema_contract.py` (seed-42 determinism).
- ✅ Validate SQLite loader and representative SQL examples — `tests/test_sqlite_loader.py`.
- ✅ Validate schema/data-dictionary alignment and module view docs —
  `tests/test_schema_contract.py`, `tests/test_progressive_views.py`.
- ✅ Validate case metadata and regulatory note structure —
  `tests/test_case_library_metadata.py`.
- ✅ Validate publication-language guardrails — `tests/test_publication_docs.py` (v0.9 + v1.0
  surfaces).
- ✅ Add a v1.0 release checklist mapping acceptance criteria to evidence —
  `docs/release/v1.0-release-checklist.md` (#255).

## Quality Gates

CI runs exactly (`.github/workflows/ci.yml`, enforced by
`tests/test_ci_quality_gates.py`):

- `uv sync --extra dev`
- `uv run ruff check .`
- `uv run pytest` (unfiltered)

v1.0 adds **no** CI commands and filters **no** tests. Optional extras (`neo4j`, `shap`)
remain out of CI.

## Verification Commands

```bash
uv sync --extra dev
uv run ruff check .
uv run pytest
```

`uv run pytest` is unfiltered — every test under `tests/` runs in CI, including the v1.0
cross-version regression gate (`tests/test_v10_acceptance_review.py`) and the data/schema
gate (`tests/test_v10_data_schema_gate.py`).

## Final Verification Evidence

v1.0 verification on the v1.0 acceptance-gate worktree (run before HITL sign-off):

- `uv sync --extra dev`: passed.
- `uv run ruff check .`: passed (`All checks passed!`).
- `uv run pytest`: passed unfiltered (full suite green, exit 0), including the v1.0
  cross-version regression loop and the data/schema gate.

## Prohibited-Content Search Evidence

The v1.0 public-facing surface is covered by automated guardrails
(`tests/test_publication_docs.py` v1.0 terminology block + `tests/test_v10_acceptance_review.py`):

- no `customer` (use Client / User); no real-bank names; no job-preparation /
  public-release framing; no real-event reconstruction claims; no imperative compliance
  wording; no assertive production-ready / certified / published claims.
- The v1.1–v1.4 extension boundary is structurally guarded: no notebook module beyond
  `09_capstone`, and the advanced tracks are confined to `docs/ROADMAP.md` § Post-1.0 and
  `docs/adr/0004-*.md`.

A maintainer can re-run the equivalent search locally:

```bash
rg -n -i "customer|julius baer|reconstructs the|reproduces the|we shipped|publicly released|is production-ready|is certified" README.md CONTEXT.md CONTRIBUTING.md docs/release/v1.0-* docs/capstone notebooks
```

## HITL Status

- **#255 (this acceptance review + release checklist):** draft, pending human sign-off. The
  repo owner must approve the frozen 10-module core list, the v1.1–v1.4 extension boundary,
  and the hardening-only publication posture. The status marker stays
  `HITL-REVIEW-REQUIRED` until signed.
- Accepting v1.0 does **not** publish the repository and does not change the separate v0.1
  publication gate (`docs/release/v0.1-publication-checklist.md`, status: ready for human
  review).

## Cross-Version Regression

`tests/test_v10_acceptance_review.py` extends the v0.9 cross-version regression pattern one
release further: it re-executes the v0.3–v0.9 notebook smoke-test modules end-to-end via
isolated `uv run pytest` subprocesses (the canonical CI invocation), proving the v1.0
hardening slices introduced no notebook-execution regression. v1.0 adds no notebooks, so the
re-executed set is identical to v0.9. It also asserts every frozen core module is present,
documented, runnable, and covered by a smoke test.

## Hand-Off

Per the PRD Cross-Version Links, **v1.0 hardens the beta path rather than expanding feature
scope.** Any work after this acceptance is post-v1.0 polish or the separate v0.1 publication
gate, not new v1.0 feature work. v1.1–v1.4 remain optional advanced extensions on top of the
v1.0 core (ADR-0004) and must preserve the v1.0 learner path. The repository remains private
until the separate v0.1 publication gate is accepted.
