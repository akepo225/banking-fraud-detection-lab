# v0.3 Delivery Context Journal

## 2026-06-09 - Integration Branch Started

- Current objective: Deliver v0.3 issues #80-#87 through issue branches and PRs into `codex/v0.3-delivery`, then open the final PR to `main`.
- Active issue/branch/PR: Integration setup on `codex/v0.3-delivery`; no issue PR yet.
- Decisions made: Created `codex/v0.3-delivery` from `origin/main`; preserve pre-existing local `.gitignore` change adding `.cursor/` and keep it out of v0.3 commits unless later confirmed in scope.
- Files changed: `.scratch/v0.3-delivery-context.md`.
- Tests run and results: Not run yet.
- Open risks/blockers: Need to read required docs and GitHub issue bodies/comments before starting #80.
- Next action: Read required repo docs and `gh issue view --comments` output for #80-#87.

## 2026-06-09 - Docs And Issues Read

- Current objective: Deliver v0.3 private-banking track through #80-#87 while preserving the canonical model, Progressive data views, and SQLite-first path.
- Active issue/branch/PR: Preparing #80 on `codex/v0.3-delivery`; no issue PR yet.
- Decisions made: Read `AGENTS.md`, `CONTEXT.md`, ADR-0001 through ADR-0005, `README.md`, `pyproject.toml`, CI workflow, `docs/ROADMAP.md`, parent PRD #46, and child issues #80-#87 with comments. Treat #46/#80 HITL language as a real approval risk; implement #80 first and record any unmergeable human-approval gate as a blocker if needed.
- Files changed: `.scratch/v0.3-delivery-context.md`.
- Tests run and results: Not run yet.
- Open risks/blockers: #80 requires a human-approval marker or comment before downstream issues are treated as unblocked; issue labels are still mostly `needs-triage`/`blocked` despite this delivery goal.
- Next action: Analyze current schema/docs/tests for #80, then create branch `codex/v03-issue-80-track-extension-contract` from `codex/v0.3-delivery`.

## 2026-06-09 - Starting Issue #80

- Current objective: Implement the v0.3/v0.4 track-extension contract and validation harness.
- Active issue/branch/PR: #80 on `codex/v03-issue-80-track-extension-contract`; no PR yet.
- Decisions made: Fit the contract into existing `docs/schema/` and detection-pattern docs. Use `PatternSpec`, `PATTERN_IDS`, and `ACTIVITY_TYPE_TO_PATTERN` as the only Detection pattern registry. Link notebook-layout guidance from `notebooks/README.md` without creating the v0.3 notebook module yet.
- Files changed: `.scratch/v0.3-delivery-context.md`.
- Tests run and results: Not run yet.
- Open risks/blockers: Human approval for #80 may still be needed before treating #81-#87 as unblocked.
- Next action: Edit schema/notebook docs and add `tests/test_track_extension_contract.py`.

## 2026-06-09 - Issue #80 Local Verification

- Current objective: Validate the v0.3/v0.4 track-extension contract before PR creation.
- Active issue/branch/PR: #80 on `codex/v03-issue-80-track-extension-contract`; no PR yet.
- Decisions made: Added contract validation for required sections, registry reuse, valid pattern IDs, HITL markers, v0.4 mirroring, prohibited terms, and index links. Tightened prohibited-term matching to avoid matching banned bank names inside unrelated words.
- Files changed: `docs/schema/track-extension-conventions.md`, `tests/test_track_extension_contract.py`, `docs/schema/README.md`, `docs/schema/detection_patterns.md`, `notebooks/README.md`, `.scratch/v0.3-delivery-context.md`.
- Tests run and results: `uv run pytest tests/test_track_extension_contract.py tests/test_detection_patterns.py` passed with 18 tests; `uv sync --extra dev` passed; `uv run ruff check .` passed; `uv run pytest` passed with 193 tests.
- Open risks/blockers: #80 still needs CodeRabbit/local review and remote PR checks; human approval may still be required by issue scope.
- Next action: Run CodeRabbit local review, then self-review/stage/commit/push/open PR if clean.

## 2026-06-09 - Issue #80 CodeRabbit Check

- Current objective: Complete local review evidence before #80 PR creation.
- Active issue/branch/PR: #80 on `codex/v03-issue-80-track-extension-contract`; no PR yet.
- Decisions made: Do not run CodeRabbit through WSL because WSL Git reports a repository-wide noisy diff unrelated to this branch, which would make review output unreliable.
- Files changed: `.scratch/v0.3-delivery-context.md`.
- Tests run and results: `coderabbit --version` failed in PowerShell because the CLI is not installed. WSL is available, but `wsl.exe ... git status` reports nearly every tracked file modified, so local CodeRabbit review is unavailable for this checkout.
- Open risks/blockers: Hosted CodeRabbit or PR review may still provide feedback after PR creation.
- Next action: Run final diff checks, stage only #80 files and journal, commit, push, and open PR to `codex/v0.3-delivery`.

## 2026-06-09 - Issue #80 PR Opened

- Current objective: Get #80 reviewed, green, and merged into `codex/v0.3-delivery`.
- Active issue/branch/PR: #80 on `codex/v03-issue-80-track-extension-contract`; PR #88 targets `codex/v0.3-delivery`.
- Decisions made: Opened PR #88 as ready for CI/hosted review so CodeRabbit can review the clean branch diff remotely.
- Files changed: `.scratch/v0.3-delivery-context.md`.
- Tests run and results: PR body records local `uv sync --extra dev`, `uv run ruff check .`, focused contract tests, full `uv run pytest`, and `git diff --check` passing before PR creation.
- Open risks/blockers: Need GitHub CI and hosted review status for PR #88; #80 human approval may still be required before downstream issues are unblocked.
- Next action: Push this journal update, monitor PR #88 checks/review feedback, and address anything actionable.

## 2026-06-09 - Issue #80 Review Feedback Fix

- Current objective: Address actionable PR #88 review feedback.
- Active issue/branch/PR: #80 on `codex/v03-issue-80-track-extension-contract`; PR #88 targets `codex/v0.3-delivery`.
- Decisions made: Accepted Codex P2 feedback that the contract should not imply current metadata may link to a future notebook path before #84 creates it. Updated the contract to require existing paths at metadata-add time and to allow the v0.3 feature-engineering path only after #84 creates it.
- Files changed: `docs/schema/track-extension-conventions.md`, `.scratch/v0.3-delivery-context.md`.
- Tests run and results: `uv run pytest tests/test_track_extension_contract.py tests/test_detection_patterns.py` passed with 18 tests; `uv run ruff check .` passed; `uv run pytest` passed with 193 tests.
- Open risks/blockers: Need PR #88 checks after push and confirmation that the Codex P2 feedback is addressed.
- Next action: Commit and push the review fix, then resolve/confirm review feedback and monitor checks.

## 2026-06-09 - Issue #80 Merged

- Current objective: Continue v0.3 delivery with #81 after the contract slice merged.
- Active issue/branch/PR: #80 complete via PR #88 merged into `codex/v0.3-delivery` at merge commit `72c1364`; preparing #81.
- Decisions made: CodeRabbit hosted review was manually triggered because auto-review skips non-default target branches. CodeRabbit reported no actionable comments after the final fix. Codex P2 feedback about future notebook `linked_modules` timing was fixed in commit `3333a0c` before merge.
- Files changed: `.scratch/v0.3-delivery-context.md` pending for the next branch.
- Tests run and results: PR #88 local verification passed (`uv sync --extra dev`, `uv run ruff check .`, focused tests, full `uv run pytest` 193 passed). PR #88 remote CI, Socket, and CodeRabbit statuses were green before merge.
- Open risks/blockers: #80 contract still carries `PENDING-HUMAN-APPROVAL`; downstream work proceeds under the user's v0.3 delivery goal, but any later repo-owner approval change must be reconciled with downstream slices.
- Next action: Create `codex/v03-issue-81-relationship-context-data` from updated `codex/v0.3-delivery`, re-read #81 implementation surface, and implement relationship-context data/view/docs/tests.

## 2026-06-09 - Starting Issue #81

- Current objective: Expand private-banking relationship context data with effective-dated relationship-manager history and a private-banking relationship-context Progressive data view.
- Active issue/branch/PR: #81 on `codex/v03-issue-81-relationship-context-data`; no PR yet.
- Decisions made: Add `relationship_manager_history` as a canonical learner-facing table after `banking_relationships` and before `accounts`, seeded from each relationship's current RM assignment. Keep existing `banking_relationships.relationship_manager_code` and `relationship_manager_assigned_at` for backward compatibility. Add `pb_relationship_context` as a Python and SQLite Progressive data view over current RM history (`effective_to` null), with source mapping and ERD/data-dictionary coverage.
- Files changed: `.scratch/v0.3-delivery-context.md`.
- Tests run and results: Not run yet for #81.
- Open risks/blockers: Need to regenerate committed sample CSVs and ensure scenario generators preserve the new table through copied dictionaries without exposing protected answer keys.
- Next action: Edit schema/generator/progressive-view/docs/tests and regenerate `data/sample/relationship_manager_history.csv`.

## 2026-06-09 - Issue #81 Local Verification

- Current objective: Validate the relationship-context data/view slice before PR creation.
- Active issue/branch/PR: #81 on `codex/v03-issue-81-relationship-context-data`; no PR yet.
- Decisions made: Kept `pb_relationship_context` one row per Banking relationship, matching the issue plan, rather than filtering to Alpine Crest rows. The view exposes `institution_name` so private-banking exercises can select Alpine Crest while preserving the canonical all-relationship surface.
- Files changed: `src/banking_fraud_lab/schema/tables.py`, `src/banking_fraud_lab/schema/__init__.py`, `src/banking_fraud_lab/generators/minimal_world.py`, `src/banking_fraud_lab/progressive_views.py`, schema docs, `data/sample/relationship_manager_history.csv`, sample README, and focused tests for generator entities, temporal semantics, and Progressive views.
- Tests run and results: Focused schema/generator/view/SQLite suite passed (`89 passed`), then post-cleanup focused schema/doc tests passed (`75 passed`); `uv sync --extra dev` passed twice; `uv run ruff check .` passed twice; `uv run pytest` passed twice (`195 passed` each run); SQLite smoke query against `pb_relationship_context` returned `(6, 6)` twice.
- Open risks/blockers: Need CodeRabbit local availability check, self-review, commit, push, PR, remote CI/review monitoring. Pre-existing `.gitignore` change remains unrelated and must not be staged.
- Next action: Run CodeRabbit local review availability check, self-review changed files, stage only #81 files plus journal, commit, push, and open PR to `codex/v0.3-delivery`.

## 2026-06-09 - Issue #81 CodeRabbit Check

- Current objective: Complete local review evidence before #81 PR creation.
- Active issue/branch/PR: #81 on `codex/v03-issue-81-relationship-context-data`; no PR yet.
- Decisions made: Treat local CodeRabbit as unavailable in this Windows checkout and use hosted CodeRabbit after PR creation, matching the #80 workflow.
- Files changed: `.scratch/v0.3-delivery-context.md`.
- Tests run and results: `coderabbit --version` and `cr --version` both failed because no executable or alias is installed; `tool_search` did not expose a local CodeRabbit review tool.
- Open risks/blockers: Hosted CodeRabbit may still raise comments after PR creation.
- Next action: Self-review changed files, run diff checks, stage only #81 files plus journal, commit, push, and open PR to `codex/v0.3-delivery`.

## 2026-06-09 - Issue #81 PR Opened

- Current objective: Get #81 reviewed, green, and merged into `codex/v0.3-delivery`.
- Active issue/branch/PR: #81 on `codex/v03-issue-81-relationship-context-data`; PR #89 targets `codex/v0.3-delivery`.
- Decisions made: Opened PR #89 as ready for CI/hosted review after local CodeRabbit proved unavailable. The implementation commit is `c451cdd`.
- Files changed: `.scratch/v0.3-delivery-context.md`.
- Tests run and results: PR body records final local verification: `uv sync --extra dev`, `uv run ruff check .`, focused tests (`89 passed`, then `75 passed` after doc cleanup), full `uv run pytest` twice (`195 passed` both times), SQLite smoke `(6, 6)`, and diff whitespace checks.
- Open risks/blockers: Need remote CI, Socket, and hosted CodeRabbit/review status for PR #89; no known implementation blocker.
- Next action: Push this journal update, monitor PR #89 checks/review feedback, trigger hosted CodeRabbit manually if it does not run, and address anything actionable.

## 2026-06-09 - Issue #81 Review Feedback

- Current objective: Address actionable CodeRabbit feedback on PR #89.
- Active issue/branch/PR: #81 on `codex/v03-issue-81-relationship-context-data`; PR #89 targets `codex/v0.3-delivery`.
- Decisions made: CodeRabbit raised two valid comments. Fix `pb_relationship_context` so Python and SQLite both deterministically choose one current RM-history row per Banking relationship when duplicate open rows exist, and tighten temporal tests to assert every Banking relationship has exactly one current RM history row.
- Files changed: `.scratch/v0.3-delivery-context.md`.
- Tests run and results: Remote CI test checks and Socket checks are green; hosted CodeRabbit status is green with two actionable comments to fix.
- Open risks/blockers: Need implement fixes, rerun focused/full verification, push, and confirm CodeRabbit/CI after the fix commit.
- Next action: Patch Progressive view selection logic and temporal tests.

## 2026-06-09 - Issue #81 Review Feedback Fix

- Current objective: Verify and push fixes for CodeRabbit feedback on PR #89.
- Active issue/branch/PR: #81 on `codex/v03-issue-81-relationship-context-data`; PR #89 targets `codex/v0.3-delivery`.
- Decisions made: Updated `pb_relationship_context` so both SQLite and pandas select the latest open RM-history row per Banking relationship using `effective_from` and `rm_history_id` ordering. Added current-history coverage assertion so every Banking relationship must have exactly one open RM-history row in generated data.
- Files changed: `src/banking_fraud_lab/progressive_views.py`, `tests/test_progressive_views.py`, `tests/test_sqlite_loader.py`, `tests/test_temporal_semantics.py`, `.scratch/v0.3-delivery-context.md`.
- Tests run and results: Focused review-fix tests passed (`37 passed`); `uv sync --extra dev` passed; `uv run ruff check .` passed; `uv run pytest` passed (`197 passed`); SQLite smoke query against `pb_relationship_context` returned `(6, 6)`.
- Open risks/blockers: Need commit/push fix, then confirm PR #89 CI and hosted CodeRabbit after the new commit.
- Next action: Stage only review-fix files plus journal, commit, push, and monitor PR #89.

## 2026-06-09 - Issue #81 Follow-Up Review Nit

- Current objective: Address remaining CodeRabbit nitpick on PR #89.
- Active issue/branch/PR: #81 on `codex/v03-issue-81-relationship-context-data`; PR #89 targets `codex/v0.3-delivery`.
- Decisions made: Accepted CodeRabbit's follow-up suggestion to use pandas `kind="stable"` instead of `kind="mergesort"` for the current RM-history sort; this preserves behavior while matching modern pandas naming.
- Files changed: `src/banking_fraud_lab/progressive_views.py`, `.scratch/v0.3-delivery-context.md`.
- Tests run and results: Focused view/SQLite tests passed (`22 passed`); `uv sync --extra dev` passed; `uv run ruff check .` passed; `uv run pytest` passed (`197 passed`); SQLite smoke query against `pb_relationship_context` returned `(6, 6)`.
- Open risks/blockers: Need commit/push nit fix, then confirm PR #89 CI/CodeRabbit again.
- Next action: Stage nit-fix files plus journal, commit, push, and monitor PR #89.

## 2026-06-09 - Issue #81 Merged

- Current objective: Continue v0.3 delivery with #82 after relationship-context data merged.
- Active issue/branch/PR: #81 complete via PR #89 merged into `codex/v0.3-delivery` at merge commit `fdf572a`; preparing #82.
- Decisions made: CodeRabbit's two substantive comments and one follow-up nit were accepted and fixed before merge. Hosted CodeRabbit, CI, and Socket checks were green at final head.
- Files changed: `.scratch/v0.3-delivery-context.md` pending for the next branch.
- Tests run and results: PR #89 local verification passed after final fix (`uv sync --extra dev`, `uv run ruff check .`, `uv run pytest` 197 passed, focused view/SQLite tests 22 passed, SQLite smoke `(6, 6)`). Remote CI test checks, Socket checks, and CodeRabbit were green before merge.
- Open risks/blockers: No #81 blockers remain. Pre-existing `.gitignore` change remains unrelated and must not be staged.
- Next action: Create `codex/v03-issue-82-transaction-context-false-positives` from updated `codex/v0.3-delivery`, re-read #82 issue context, and implement private-banking transaction context plus false-positive data.

## 2026-06-09 - Starting Issue #82

- Current objective: Add private-banking transaction context, AUM baseline, Alpine Crest counterparties, richer transaction typologies, and multiple legitimate false-positive examples.
- Active issue/branch/PR: #82 on `codex/v03-issue-82-transaction-context-false-positives`; no PR yet.
- Decisions made: Read required repo docs/ADRs and `gh issue view 82 --comments`; use the issue body as source of truth and CodeRabbit's plan only as implementation guidance. Add `aum_chf` directly to `banking_relationships`, reuse `payment_beneficiaries` for Alpine Crest counterparties, preserve protected answer-key separation, and keep private-banking activity types mapped through existing Detection pattern IDs unless new activity types become necessary.
- Files changed: `.scratch/v0.3-delivery-context.md`.
- Tests run and results: Not run yet for #82.
- Open risks/blockers: Need to inspect the current generator and scenario injector carefully because #81 added `relationship_manager_history` and the sample data must be regenerated without staging the unrelated `.gitignore` change.
- Next action: Patch schema/generator/scenario/docs/tests for #82, regenerate sample CSVs, then run focused and full verification.

## 2026-06-09 - Issue #82 Implementation And Focused Tests

- Current objective: Validate #82 private-banking transaction context before full PR verification.
- Active issue/branch/PR: #82 on `codex/v03-issue-82-transaction-context-false-positives`; no PR yet.
- Decisions made: Added `banking_relationships.aum_chf` rather than a separate AUM table; generated one User per Client so Alpine Crest counterparties can satisfy the existing non-null `payment_beneficiaries.added_by_user_id` contract; reused `private_banking_high_value` for legitimate false positives; exposed `aum_chf` through `pb_relationship_context` for Python and SQLite paths.
- Files changed: `src/banking_fraud_lab/schema/tables.py`, `src/banking_fraud_lab/generators/minimal_world.py`, `src/banking_fraud_lab/generators/private_banking.py`, `src/banking_fraud_lab/progressive_views.py`, focused tests, schema docs, sample CSVs, and sample README.
- Tests run and results: Focused suite passed: `uv run pytest tests/test_schema_contract.py tests/test_generator_entities.py tests/test_private_banking_scenario.py tests/test_progressive_views.py tests/test_sqlite_loader.py::test_sqlite_private_banking_transaction_context_is_queryable tests/test_generator_determinism.py::test_private_banking_false_positive_generation_is_deterministic` (`67 passed`).
- Open risks/blockers: Need run required `uv sync --extra dev`, ruff, full pytest, SQLite smoke, notebook smoke where relevant, CodeRabbit availability check, and self-review. Pre-existing `.gitignore` change remains unrelated.
- Next action: Run full local verification and inspect changed files before staging.

## 2026-06-09 - Issue #82 Local Verification

- Current objective: Complete #82 local verification before CodeRabbit/self-review and PR creation.
- Active issue/branch/PR: #82 on `codex/v03-issue-82-transaction-context-false-positives`; no PR yet.
- Decisions made: Treat foundation and private-banking notebook smoke tests as relevant because the canonical generator and private-banking baseline data changed.
- Files changed: Same #82 implementation/docs/tests/sample set plus `.scratch/v0.3-delivery-context.md`.
- Tests run and results: `uv sync --extra dev` passed; `uv run ruff check .` passed; full `uv run pytest` passed (`204 passed`); notebook smoke `uv run pytest tests/test_private_banking_notebook.py tests/test_foundations_notebook.py` passed (`2 passed`); SQLite smoke returned Alpine Crest `(10, 6, 7)` for transactions/distinct typologies/linked counterparties and `pb_relationship_context` `(6, 207845.12)` for rows/min AUM.
- Open risks/blockers: Need CodeRabbit local availability check, diff self-review, staging/commit/push/PR, hosted review/CI monitoring. Pre-existing `.gitignore` change remains unrelated.
- Next action: Run CodeRabbit availability check and self-review changed files.

## 2026-06-09 - Issue #82 CodeRabbit Check And Self-Review

- Current objective: Commit and open PR #82 after local review.
- Active issue/branch/PR: #82 on `codex/v03-issue-82-transaction-context-false-positives`; no PR yet.
- Decisions made: Local CodeRabbit remains unavailable in this Windows checkout (`coderabbit` and `cr` commands not found), so hosted CodeRabbit will be triggered after PR creation. Self-review adjusted the private-banking transaction type cycle to the intended 20-row 60/15/10/5/5/5 mix while preserving the tiny sample typology coverage.
- Files changed: Same #82 implementation/docs/tests/sample set plus `.scratch/v0.3-delivery-context.md`.
- Tests run and results: After the final transaction-cycle/test update, `uv run ruff check .` passed; focused private-banking tests passed (`9 passed`); full `uv run pytest` passed (`204 passed`); notebook smoke passed (`2 passed`); SQLite smoke still returned `(10, 6, 7)` and `(6, 207845.12)`.
- Open risks/blockers: Need commit, push, PR to `codex/v0.3-delivery`, hosted review, and remote CI. Pre-existing `.gitignore` change remains unrelated and unstaged.
- Next action: Stage only #82 files plus journal, commit, push, and open PR targeting `codex/v0.3-delivery`.

## 2026-06-09 - Issue #82 PR Opened

- Current objective: Get #82 reviewed, green, and merged into `codex/v0.3-delivery`.
- Active issue/branch/PR: #82 on `codex/v03-issue-82-transaction-context-false-positives`; PR #90 targets `codex/v0.3-delivery`.
- Decisions made: Opened PR #90 as ready for CI/hosted review after local CodeRabbit proved unavailable. Implementation commit is `ba56c02`.
- Files changed: `.scratch/v0.3-delivery-context.md`.
- Tests run and results: PR body records `uv sync --extra dev`, `uv run ruff check .`, full `uv run pytest` (`204 passed`), focused #82 tests (`67 passed`), notebook smoke (`2 passed`), and SQLite smoke `(10, 6, 7)` / `(6, 207845.12)`.
- Open risks/blockers: Need remote CI, Socket, hosted CodeRabbit/review status for PR #90; no known implementation blocker. Pre-existing `.gitignore` change remains unrelated.
- Next action: Push this journal update, trigger hosted CodeRabbit if needed, monitor PR #90 checks/review feedback, and address anything actionable.

## 2026-06-09 - Issue #82 Review Feedback Fix

- Current objective: Fix actionable review feedback on PR #90 and restore green checks.
- Active issue/branch/PR: #82 on `codex/v03-issue-82-transaction-context-false-positives`; PR #90 targets `codex/v0.3-delivery`.
- Decisions made: Accepted Codex P2 feedback that creating Users for every Client must not assign NovaBank-branded session telemetry to Alpine Crest Users. Kept Alpine Crest Users for private-banking counterparty ownership, but constrained generated foundation sessions to NovaBank Users and added a regression assertion.
- Files changed: `src/banking_fraud_lab/generators/minimal_world.py`, `tests/test_generator_entities.py`, regenerated sample CSVs with session/user references, and `.scratch/v0.3-delivery-context.md`.
- Tests run and results: `uv run ruff check .` passed; focused review-fix tests passed (`10 passed`); full `uv run pytest` passed (`204 passed`); notebook smoke passed (`2 passed`); SQLite smoke returned `(10, 6, 7)` and `(6, 207845.12)`.
- Open risks/blockers: Need commit/push fix, then confirm PR #90 CI/CodeRabbit/review status again. Pre-existing `.gitignore` change remains unrelated.
- Next action: Stage review-fix files plus journal, commit, push, and monitor PR #90.

## 2026-06-09 - Issue #82 CodeRabbit Refactor Fix

- Current objective: Address CodeRabbit's actionable helper-extraction comment on PR #90.
- Active issue/branch/PR: #82 on `codex/v03-issue-82-transaction-context-false-positives`; PR #90 targets `codex/v0.3-delivery`.
- Decisions made: Accepted CodeRabbit's suggestion to centralize repeated `pd.isna(...) -> None` beneficiary normalization in `_normalize_beneficiary_id`.
- Files changed: `src/banking_fraud_lab/generators/private_banking.py`, `.scratch/v0.3-delivery-context.md`.
- Tests run and results: `uv run ruff check .` passed; focused private-banking tests passed (`8 passed`); full `uv run pytest` passed (`204 passed`); notebook smoke passed (`2 passed`); SQLite smoke returned `(10, 6, 7)` and `(6, 207845.12)`.
- Open risks/blockers: Need commit/push helper fix, then confirm PR #90 CI/CodeRabbit/review status. Pre-existing `.gitignore` change remains unrelated.
- Next action: Stage helper fix plus journal, commit, push, and monitor PR #90.

## 2026-06-09 - Issue #82 Merged

- Current objective: Continue v0.3 delivery with #83 after private-banking transaction context merged.
- Active issue/branch/PR: #82 complete via PR #90 merged into `codex/v0.3-delivery` at merge commit `9a93a07`; preparing #83.
- Decisions made: Accepted and fixed Codex P2 session-telemetry feedback and CodeRabbit beneficiary-normalization feedback before merge. Final hosted CodeRabbit, GitHub Actions, and Socket checks were green at head `7ae1fa4`.
- Files changed: `.scratch/v0.3-delivery-context.md` pending for the next branch.
- Tests run and results: Final #82 local verification passed after all review fixes: `uv run ruff check .`, focused tests (`8 passed` and earlier `10 passed`), full `uv run pytest` (`204 passed`), notebook smoke (`2 passed`), and SQLite smoke `(10, 6, 7)` / `(6, 207845.12)`. Remote PR #90 test, Socket, and CodeRabbit checks were green before merge.
- Open risks/blockers: No #82 blockers remain. Pre-existing `.gitignore` change remains unrelated and must not be staged.
- Next action: Create `codex/v03-issue-83-feature-library-sql-exercises` from updated `codex/v0.3-delivery`, re-read #83 issue context, and implement the private-banking feature library and SQL exercises.

## 2026-06-09 - Starting Issue #83

- Current objective: Implement the private-banking feature library and SQLite-first feature exercises that downstream v0.3 notebooks will consume.
- Active issue/branch/PR: #83 on `codex/v03-issue-83-feature-library-sql-exercises`; no PR yet.
- Decisions made: Read required repo docs/ADRs, `docs/ROADMAP.md`, v0.3 PRD, and `gh issue view 83 --comments`; treat CodeRabbit's issue plan as guidance only. Add a new `banking_fraud_lab.features` package with metadata linked to existing Detection pattern IDs, deterministic pandas calculators over canonical tables, and standalone SQL exercises 06-08 against the learner SQLite database. Keep SQL examples SQLite-only and Alpine Crest scoped; do not expose protected answer keys.
- Files changed: `.scratch/v0.3-delivery-context.md`.
- Tests run and results: Not run yet for #83.
- Open risks/blockers: Need to keep feature formulas simple enough for learners and deterministic enough for tests while avoiding notebook-only logic. Local CodeRabbit availability is expected to remain unavailable, so hosted review will be triggered after PR creation.
- Next action: Add feature specs/calculators, SQL exercises, docs, exports, and focused tests.

## 2026-06-09 - Issue #83 Implementation And Focused Tests

- Current objective: Validate the private-banking feature library and SQL exercises before full PR verification.
- Active issue/branch/PR: #83 on `codex/v03-issue-83-feature-library-sql-exercises`; no PR yet.
- Decisions made: Implemented feature-family specs and calculators as a new `banking_fraud_lab.features` package rather than changing generated schema or sample data. `build_private_banking_features()` returns Alpine Crest transaction-level features and excludes protected answer keys. SQL exercises 06-08 remain single-statement SQLite examples. Exercise 06 uses both account-balance traceability and `banking_relationships.aum_chf`.
- Files changed: `src/banking_fraud_lab/features/`, `src/banking_fraud_lab/__init__.py`, `sql/examples/06_private_banking_value_features.sql`, `sql/examples/07_private_banking_context_features.sql`, `sql/examples/08_private_banking_relationship_features.sql`, `docs/schema/features.md`, `docs/schema/README.md`, `sql/README.md`, `README.md`, `tests/test_private_banking_features.py`, `tests/test_sql_foundations_exercises.py`, `tests/test_sqlite_loader.py`, `tests/test_package_import.py`, `.scratch/v0.3-delivery-context.md`.
- Tests run and results: Focused tests passed: `uv run pytest tests/test_private_banking_features.py tests/test_sql_foundations_exercises.py tests/test_sqlite_loader.py::test_representative_sql_examples_execute_successfully tests/test_package_import.py` (`13 passed`). Scoped Ruff passed: `uv run ruff check src/banking_fraud_lab/features tests/test_private_banking_features.py tests/test_sql_foundations_exercises.py tests/test_package_import.py`.
- Open risks/blockers: Need required `uv sync --extra dev`, full Ruff, full pytest, SQL smoke, local CodeRabbit availability check, and self-review. Pre-existing `.gitignore` change remains unrelated and must not be staged.
- Next action: Run full local verification for #83.

## 2026-06-09 - Issue #83 Local Verification And Self-Review

- Current objective: Commit and open PR #83 after local verification.
- Active issue/branch/PR: #83 on `codex/v03-issue-83-feature-library-sql-exercises`; no PR yet.
- Decisions made: Accepted self-review adjustment to align Python and SQL new-counterparty behavior around first-observed or recently created counterparties. Scoped relationship-manager concentration to Alpine Crest relationships when institution context is available. Local CodeRabbit remains unavailable, so hosted CodeRabbit will be triggered after PR creation.
- Files changed: Same #83 implementation/docs/tests set plus `.scratch/v0.3-delivery-context.md`.
- Tests run and results: Final local verification passed: `uv sync --extra dev`; `uv run ruff check .`; `uv run pytest` (`212 passed`); focused feature tests (`6 passed`); focused SQL/package suite (`13 passed` before final self-review); SQL runner smoke for `06_private_banking_value_features.sql`, `07_private_banking_context_features.sql`, and `08_private_banking_relationship_features.sql` against `C:\Users\yvesa\AppData\Local\Temp\v03_issue83_smoke.sqlite`; `git diff --check` passed with line-ending warnings only. Local CodeRabbit checks `coderabbit --version` and `cr --version` both failed because the commands are not installed.
- Open risks/blockers: Need stage only #83 files plus journal, excluding the unrelated `.gitignore` change, then commit, push, open PR to `codex/v0.3-delivery`, trigger hosted CodeRabbit, and monitor remote CI/review.
- Next action: Stage #83 files plus journal, commit, push, and open PR targeting `codex/v0.3-delivery`.

## 2026-06-09 - Issue #83 PR Opened

- Current objective: Get #83 reviewed, green, and merged into `codex/v0.3-delivery`.
- Active issue/branch/PR: #83 on `codex/v03-issue-83-feature-library-sql-exercises`; PR #91 targets `codex/v0.3-delivery`.
- Decisions made: Opened PR #91 with implementation commit `71e9bef`. CodeRabbit auto-review skipped because the base branch is not the default branch, so a manual hosted review was requested with `@coderabbitai review`.
- Files changed: `.scratch/v0.3-delivery-context.md`.
- Tests run and results: PR body records local `uv sync --extra dev`, `uv run ruff check .`, full `uv run pytest` (`212 passed`), focused feature tests (`6 passed`), focused SQL/package suite (`13 passed` before final self-review patch), SQL runner smoke for examples 06-08, and `git diff --check`.
- Open risks/blockers: GitHub Actions test checks were in progress at PR creation; Socket checks were green; hosted CodeRabbit review is pending after manual trigger. Pre-existing `.gitignore` change remains unrelated.
- Next action: Push this journal update, monitor PR #91 CI and hosted review feedback, and address anything actionable.

## 2026-06-09 - Issue #83 Review Feedback Fix

- Current objective: Address actionable PR #91 review feedback and restore green checks.
- Active issue/branch/PR: #83 on `codex/v03-issue-83-feature-library-sql-exercises`; PR #91 targets `codex/v0.3-delivery`.
- Decisions made: Accepted Codex P2 feedback that SQL exercise 07 should use actual date-bounded velocity windows instead of `ROWS BETWEEN` counts while naming outputs 7-day and 30-day. Replaced row-count windows with `julianday(booked_at)` correlated subqueries that match the pandas calculator's date-window semantics.
- Files changed: `sql/examples/07_private_banking_context_features.sql`, `tests/test_sql_foundations_exercises.py`, `.scratch/v0.3-delivery-context.md`.
- Tests run and results: Focused SQL tests passed (`6 passed`); SQL runner smoke for `07_private_banking_context_features.sql` passed against `C:\Users\yvesa\AppData\Local\Temp\v03_issue83_smoke.sqlite`; `uv run ruff check .` passed; `uv run pytest` passed (`212 passed`).
- Open risks/blockers: Need commit/push review fix, then confirm PR #91 CI, CodeRabbit, and merge state again.
- Next action: Stage review-fix files plus journal, commit, push, and monitor PR #91.

## 2026-06-09 - Issue #83 Merged

- Current objective: Continue v0.3 delivery with #84 after the feature library and SQL exercises merged.
- Active issue/branch/PR: #83 complete via PR #91 merged into `codex/v0.3-delivery` at merge commit `cc40643`; preparing #84.
- Decisions made: Codex P2 velocity-window feedback was fixed in commit `4a36cfa` before merge. Hosted CodeRabbit completed successfully after manual trigger; no actionable CodeRabbit comments remained. GitHub Actions, Socket, and CodeRabbit checks were green and merge state was clean before merge.
- Files changed: `.scratch/v0.3-delivery-context.md` pending for the next branch.
- Tests run and results: Final #83 local verification after review fix passed: `uv sync --extra dev`, `uv run ruff check .`, `uv run pytest` (`212 passed`), focused SQL tests (`6 passed`), and SQL runner smoke for the corrected context-feature exercise. Remote PR #91 test, Socket, and CodeRabbit checks were green before merge.
- Open risks/blockers: No #83 blockers remain. Pre-existing `.gitignore` change remains unrelated and must not be staged.
- Next action: Create `codex/v03-issue-84-feature-engineering-notebook` from updated `codex/v0.3-delivery`, re-read #84 issue context, and build the private-banking feature engineering notebook.

## 2026-06-09 - Starting Issue #84

- Current objective: Build the `04_private_banking_feature_engineering` notebook module for Alpine Crest Private Bank.
- Active issue/branch/PR: #84 on `codex/v03-issue-84-feature-engineering-notebook`; no PR yet.
- Decisions made: Read required repo docs/ADRs, `docs/ROADMAP.md`, v0.3 PRD, `notebooks/README.md`, and `gh issue view 84 --comments`. Treat the issue body as source of truth: the notebook must consume the #83 feature library and SQLite/Python learner paths, not reimplement feature logic inline as the stale CodeRabbit plan suggested. Use tiny seed-42 data, keep protected answer keys absent from learner-facing outputs, and explicitly map feature families to `PATTERN_IDS`.
- Files changed: `.scratch/v0.3-delivery-context.md`.
- Tests run and results: Not run yet for #84.
- Open risks/blockers: Need to create a runnable notebook with enough output for smoke testing while keeping cells concise and avoiding protected answer-key exposure. Pre-existing `.gitignore` change remains unrelated and must not be staged.
- Next action: Create the notebook module directory, notebook, smoke test, and README/module documentation updates.

## 2026-06-09 - Issue #84 Implementation Plan

- Current objective: Implement the private-banking feature-engineering notebook as a consumer of the #83 feature library and SQLite examples.
- Active issue/branch/PR: #84 on `codex/v03-issue-84-feature-engineering-notebook`; no PR yet.
- Decisions made: Mirror existing featured notebook smoke-test mechanics with `jupyter nbconvert --execute`. The notebook will generate tiny seed-42 Alpine Crest data, build learner-facing views, load an in-memory SQLite database, run SQL examples 06-08, call `build_private_banking_features()`, and show `PRIVATE_BANKING_FEATURE_FAMILIES` to `PATTERN_IDS` mapping without displaying protected answer keys.
- Files changed: `.scratch/v0.3-delivery-context.md`.
- Tests run and results: Not run yet for #84.
- Open risks/blockers: Need to ensure notebook path discovery works under nbconvert from the repo root and that display outputs remain learner-facing only.
- Next action: Add `notebooks/04_private_banking_feature_engineering/alpine_crest_feature_engineering.ipynb`, a focused smoke test, and docs links.

## 2026-06-09 - Issue #84 Implementation And Focused Tests

- Current objective: Run full local verification for the private-banking feature-engineering notebook.
- Active issue/branch/PR: #84 on `codex/v03-issue-84-feature-engineering-notebook`; no PR yet.
- Decisions made: Added the notebook as a consumer of `build_private_banking_features()`, `PRIVATE_BANKING_FEATURE_FAMILIES`, learner-facing tables, and SQL examples 06-08. Kept feature calculations in the library/SQL paths rather than duplicating them in notebook-only helper logic. Added a dedicated nbconvert smoke test and updated notebook, feature, SQL, and roadmap docs for discoverability.
- Files changed: `notebooks/04_private_banking_feature_engineering/alpine_crest_feature_engineering.ipynb`, `tests/test_private_banking_feature_engineering_notebook.py`, `notebooks/README.md`, `docs/schema/features.md`, `sql/README.md`, `docs/ROADMAP.md`, `.scratch/v0.3-delivery-context.md`.
- Tests run and results: Focused notebook smoke passed (`1 passed`). Focused related suite passed: `uv run pytest tests/test_private_banking_feature_engineering_notebook.py tests/test_private_banking_features.py tests/test_sql_foundations_exercises.py::test_private_banking_sql_exercises_cover_feature_topics tests/test_sql_foundations_exercises.py::test_private_banking_sql_exercises_return_alpine_crest_rows tests/test_sql_foundations_exercises.py::test_sql_guide_and_foundations_notebook_reference_expanded_exercises` (`10 passed`).
- Open risks/blockers: Need required `uv sync --extra dev`, full Ruff, full pytest, self-review, and local CodeRabbit availability check. Pre-existing `.gitignore` change remains unrelated and must not be staged.
- Next action: Run required full local verification for #84.

## 2026-06-09 - Issue #84 Local Verification And Self-Review

- Current objective: Commit and open PR #84 after local verification.
- Active issue/branch/PR: #84 on `codex/v03-issue-84-feature-engineering-notebook`; no PR yet.
- Decisions made: Self-review tightened the notebook so `build_private_banking_features()` consumes `learner_tables` instead of the full generated mapping. The notebook still asserts that protected keys are excluded but does not display protected answer-key rows. Local CodeRabbit remains unavailable (`coderabbit` and `cr` commands are not installed), so hosted CodeRabbit will be triggered after PR creation.
- Files changed: Same #84 notebook/docs/test set plus `.scratch/v0.3-delivery-context.md`.
- Tests run and results: Final local verification passed: `uv sync --extra dev`; `uv run ruff check .`; `uv run pytest` (`213 passed`); focused notebook/feature/SQL suite after self-review (`8 passed`); prior focused #84 suite (`10 passed`); `git diff --check -- . ':!.gitignore'` passed with line-ending warnings only.
- Open risks/blockers: Need stage only #84 files plus journal, excluding the unrelated `.gitignore` change, then commit, push, open PR to `codex/v0.3-delivery`, trigger hosted CodeRabbit, and monitor remote CI/review.
- Next action: Stage #84 files plus journal, commit, push, and open PR targeting `codex/v0.3-delivery`.

## 2026-06-09 - Issue #84 PR Opened

- Current objective: Get #84 reviewed, green, and merged into `codex/v0.3-delivery`.
- Active issue/branch/PR: #84 on `codex/v03-issue-84-feature-engineering-notebook`; PR #92 targets `codex/v0.3-delivery`.
- Decisions made: Opened PR #92 with implementation commit `a9a8c2e`. Local CodeRabbit is unavailable, so hosted CodeRabbit was manually requested with `@coderabbitai review`.
- Files changed: `.scratch/v0.3-delivery-context.md`.
- Tests run and results: PR body records local `uv sync --extra dev`, `uv run ruff check .`, full `uv run pytest` (`213 passed`), focused notebook/feature/SQL suite (`8 passed`), prior focused suite (`10 passed`), and `git diff --check -- . ':!.gitignore'`.
- Open risks/blockers: Need monitor GitHub Actions, Socket, and hosted CodeRabbit for PR #92. Pre-existing `.gitignore` change remains unrelated and must not be staged.
- Next action: Push this journal update, monitor PR #92 CI and hosted review feedback, and address anything actionable.

## 2026-06-09 - Issue #84 Merged

- Current objective: Continue v0.3 delivery with #85 after the feature-engineering notebook merged.
- Active issue/branch/PR: #84 complete via PR #92 merged into `codex/v0.3-delivery` at merge commit `80ed8e1`; preparing #85.
- Decisions made: PR #92 was clean and mergeable. GitHub Actions test checks, Socket checks, and CodeRabbit status were green; CodeRabbit produced the expected non-default-base skip notice plus no actionable review comments after manual trigger.
- Files changed: `.scratch/v0.3-delivery-context.md` pending for the next branch.
- Tests run and results: Final #84 local verification passed: `uv sync --extra dev`, `uv run ruff check .`, `uv run pytest` (`213 passed`), and focused notebook/feature/SQL suite (`8 passed`). Remote PR #92 test, Socket, and CodeRabbit checks were green before merge.
- Open risks/blockers: No #84 blockers remain. Pre-existing `.gitignore` change remains unrelated and must not be staged.
- Next action: Create `codex/v03-issue-85-supervised-baseline-threshold-tuning` from updated `codex/v0.3-delivery`, re-read #85 issue context, and build the private-banking supervised baseline and threshold-tuning notebook.

## 2026-06-09 - Starting Issue #85

- Current objective: Build the Alpine Crest supervised baseline and threshold-tuning notebook for v0.3.
- Active issue/branch/PR: #85 on `codex/v03-issue-85-supervised-baseline-threshold-tuning`; no PR yet.
- Decisions made: Read required repo docs/ADRs, `docs/ROADMAP.md`, v0.3 PRD, track-extension conventions, notebook guide, and `gh issue view 85 --comments`. Treat the issue body as source of truth and use the CodeRabbit plan only where it matches current code. The notebook should live under `notebooks/04_private_banking_feature_engineering/`, consume `build_private_banking_features()` output, train a simple deterministic sklearn supervised baseline, and use `evaluate_alert_scores()` for thresholds, capacity, costs, PR-AUC, and limitation wording. Use `case_outcomes` for supervised labels; do not make learners inspect protected answer keys.
- Files changed: `.scratch/v0.3-delivery-context.md`.
- Tests run and results: Not run yet for #85.
- Open risks/blockers: Need fit the model on tiny data without overstating generalization, keep interpretation tied to Detection patterns, and add deterministic notebook smoke/contract tests. Pre-existing `.gitignore` change remains unrelated and must not be staged.
- Next action: Create `alpine_crest_supervised_baseline.ipynb`, tests, and docs links.

## 2026-06-09 - Issue #85 Implementation And Focused Tests

- Current objective: Run full local verification for the private-banking supervised baseline and threshold-tuning notebook.
- Active issue/branch/PR: #85 on `codex/v03-issue-85-supervised-baseline-threshold-tuning`; no PR yet.
- Decisions made: Added a single featured `alpine_crest_supervised_baseline.ipynb` notebook under the v0.3 private-banking module. It consumes `build_private_banking_features(learner_tables)`, trains a deterministic logistic-regression baseline on feature-library outputs, maps coefficients back to Detection pattern IDs, evaluates thresholds with `evaluate_alert_scores()`, and interprets legitimate high-value false positives without exposing protected answer-key rows. Added notebook smoke and source-contract tests plus docs links in notebook, feature, evaluation, and track-convention docs.
- Files changed: `notebooks/04_private_banking_feature_engineering/alpine_crest_supervised_baseline.ipynb`, `tests/test_private_banking_supervised_baseline_notebook.py`, `notebooks/README.md`, `docs/schema/features.md`, `docs/evaluation/metrics.md`, `docs/schema/track-extension-conventions.md`, `.scratch/v0.3-delivery-context.md`.
- Tests run and results: New notebook tests passed (`2 passed`). Focused related suite passed: `uv run pytest tests/test_private_banking_supervised_baseline_notebook.py tests/test_evaluation_metrics.py tests/test_private_banking_feature_engineering_notebook.py tests/test_private_banking_features.py` (`22 passed`).
- Open risks/blockers: Need required `uv sync --extra dev`, full Ruff, full pytest, self-review, and local CodeRabbit availability check. Pre-existing `.gitignore` change remains unrelated and must not be staged.
- Next action: Run required full local verification for #85.

## 2026-06-09 - Issue #85 Local Verification And Self-Review

- Current objective: Commit and open PR #85 after local verification.
- Active issue/branch/PR: #85 on `codex/v03-issue-85-supervised-baseline-threshold-tuning`; no PR yet.
- Decisions made: Self-review found no code changes needed. The notebook is unexecuted in git, uses learner-facing feature inputs, calls the shared evaluation utility, displays case-outcome labels only for the supervised-learning path, and does not display protected answer-key rows. Local CodeRabbit remains unavailable (`coderabbit` and `cr` commands are not installed), so hosted CodeRabbit will be triggered after PR creation.
- Files changed: Same #85 notebook/docs/test set plus `.scratch/v0.3-delivery-context.md`.
- Tests run and results: Final local verification passed: `uv sync --extra dev`; `uv run ruff check .`; `uv run pytest` (`215 passed`); focused #85 suite (`22 passed`); new notebook tests (`2 passed`); `git diff --check -- . ':!.gitignore'` passed with line-ending warnings only.
- Open risks/blockers: Need stage only #85 files plus journal, excluding the unrelated `.gitignore` change, then commit, push, open PR to `codex/v0.3-delivery`, trigger hosted CodeRabbit, and monitor remote CI/review.
- Next action: Stage #85 files plus journal, commit, push, and open PR targeting `codex/v0.3-delivery`.
